# =============================================================================
# College Portal Backend - Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# FastAPI - The web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0      # ASGI server to run FastAPI

# Database
sqlalchemy[asyncio]==2.0.25    # ORM with async support
asyncpg==0.29.0                # Async PostgreSQL driver
greenlet==3.0.3                # Required for SQLAlchemy async

# Settings & Environment
pydantic==2.5.3                # Data validation (used by FastAPI)
pydantic-settings==2.1.0       # Settings management from .env

# Authentication
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4            # Password hashing

# Email (for future password reset feature)
# aiosmtplib==3.0.1

# Utilities
python-multipart==0.0.6        # Required for form data (login)
email-validator==2.1.0         # Email validation for Pydantic
cloudinary==1.36.0             # Cloudinary SDK
